#coding:utf-8

import pdfkit
import random
from FW.models import Words, Units, 
from jinja2 import NativeEnvironment

def create_list(W_count,U_list,is_rand):


def create_pdf(W_count, U_list, is_rand=True):
    L = create_list(W_count,U_list,is_rand)
